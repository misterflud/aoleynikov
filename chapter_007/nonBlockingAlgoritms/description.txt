Programs:
1. Cache. Saves models without synchronize. This is based on non-blocking algorithms. Classes:
	Cache.java is store;
	OptimisticException.java is exception, when 2 threads want change cache in the same time;
	Model.java;
	CacheTest.java is test;
	   